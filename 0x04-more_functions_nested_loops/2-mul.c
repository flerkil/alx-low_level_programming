#include "main.h"

/**
* mul - causes an infinite loop
* @a: the int we will mul
* @b: the int we will mul
* Return: 1 if c  is a digit and 0 otherwise.
*/

int mul(int a, int b)
{
int c;

c = a * b;
return (c);
}
